package com.example.scl.model.repository;

import com.example.scl.model.entity.Genero;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GenereoRepository extends JpaRepository <Genero, Long> {
}
